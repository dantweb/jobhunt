.env
.venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
htmlcov
.coverage
*.db
*.sqlite*
*.egg-info
.DS_Store

# Personal data — must not land in the public repo.
# Anchor patterns to repo root with leading `/` so they don't match
# package paths like `jobhunt/cv/` (the CV-parsing package).
/config.toml
/var/
/cv/
*.cv.pdf
/.claude/
/.idea/
